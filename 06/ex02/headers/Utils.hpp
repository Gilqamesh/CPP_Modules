#ifndef UTILS_HPP
# define UTILS_HPP

# include "A.hpp"
# include "B.hpp"
# include "C.hpp"

Base	*generate(void);
void	identify(Base *);
void	identify(Base &);
int		getRand(int, int);

#endif /* UTILS_HPP */
